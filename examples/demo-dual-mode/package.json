{
  "name": "demo-dual-mode",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "demo-dual-mode（A3）：双模式对照——同一条含坏行的指令流：在线 feedLine 坏行隔离继续 vs 离线 feedBatch 整批原子拒绝；共享同一套校验规则库",
  "scripts": {
    "start": "node scripts/run.mjs",
    "test": "node --test --test-isolation=none 'test/*.test.ts'"
  },
  "dependencies": {
    "@l2dp/driver": "*"
  }
}
