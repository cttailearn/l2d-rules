{
  "name": "@l2dp/demo-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "小型使用应用 demo：Live2D 聊天助手（看板娘/VTuber 小应用）——聊天输入 → 两跳决策 + 确定性应答 → JSONL 驱动角色说话/表情/动作，SceneStage 场景 + 换装 + 语音口型。浏览器（Vite）与无头（Node）同核。",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node scripts/run.mjs",
    "test": "node --test --test-isolation=none \"test/**/*.test.ts\""
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
