{
  "name": "demo-llm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "demo-llm（A4）：真实/模拟 LLM 驱动演示——两跳（第一跳本地规则、第二跳 OpenAIProvider 决策，LLM_API_KEY 可选），无 key 时 MockProvider 兜底；hop 指标 + audit 日志",
  "scripts": {
    "start": "node scripts/run.mjs",
    "test": "node --test --test-isolation=none 'test/*.test.ts'"
  },
  "dependencies": {
    "@l2dp/driver": "*",
    "@l2dp/engine": "*"
  }
}
