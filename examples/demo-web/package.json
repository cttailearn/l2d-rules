{
  "name": "@l2dp/demo-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "M6 端到端 demo：浏览器输入 JSONL → 自研引擎实时动作（Vite，零构建产物直编 TS 源码）。支持真实纹理：?model=haru-full.l2dm（自包含 .l2dm，内嵌 Haru 纹理）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:haru": "node scripts/gen-real.mjs",
    "prepare:official": "node scripts/prepare-official.mjs",
    "test:e2e": "playwright test --config ./e2e/playwright.config.ts"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
